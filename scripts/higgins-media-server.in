#!/usr/bin/env python

from sys import path, argv
path.insert(0, "@pythondir@")

from higgins import Application
from higgins.logging import log_error

if __name__ == '__main__':
    app = Application(argv[1:])
    app.run()
